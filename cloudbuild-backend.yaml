substitutions:
  _REGION: europe-west4
  _GKE_CLUSTER: deployment-1-cluster
  _GKE_NAMESPACE: rag-yeni
  _AR_REPOSITORY: rag-artifact-registery
  _SERVICE_NAME: rag-langchain
  _AR_HOST: europe-west3-docker.pkg.dev

options:
  machineType: E2_HIGHCPU_8
  logging: CLOUD_LOGGING_ONLY
  env:
    - DOCKER_BUILDKIT=1

steps:
  # ── 1a. Lint + Unit Tests ─────────────────────────────────────────────────
  - id: lint-and-test
    name: python:3.11-slim
    entrypoint: bash
    args:
      - -c
      - |
        pip install --quiet uv
        uv sync --frozen --group dev
        uv run flake8 RAG tests --max-line-length=120 --extend-ignore=E501,W503
        uv run pytest tests/ -v --tb=short

  # ── 1b. Build + Push (parallel with lint) ────────────────────────────────
  - id: build-and-push
    name: gcr.io/cloud-builders/docker
    entrypoint: bash
    waitFor: ['-']
    args:
      - -c
      - |
        IMAGE="${_AR_HOST}/$PROJECT_ID/$_AR_REPOSITORY/$_SERVICE_NAME"
        docker pull "$$IMAGE:latest" || true
        docker build \
          --cache-from "$$IMAGE:latest" \
          --build-arg BUILDKIT_INLINE_CACHE=1 \
          -t "$$IMAGE:$SHORT_SHA" \
          -t "$$IMAGE:latest" \
          .
        docker push --all-tags "$$IMAGE"

  # ── 2. Deploy Backend to GKE (waits for both lint AND build) ─────────────
  - id: deploy-to-gke
    name: gcr.io/google.com/cloudsdktool/cloud-sdk:latest
    entrypoint: bash
    waitFor: [lint-and-test, build-and-push]
    args:
      - -c
      - |
        export USE_GKE_GCLOUD_AUTH_PLUGIN=True
        gcloud container clusters get-credentials "$_GKE_CLUSTER" \
          --region "$_REGION" \
          --project "$PROJECT_ID"

        IMAGE="${_AR_HOST}/$PROJECT_ID/$_AR_REPOSITORY/$_SERVICE_NAME"
        sed -i "s#europe-west3-docker.pkg.dev/PROJECT_ID/$_AR_REPOSITORY/$_SERVICE_NAME:latest#$$IMAGE:$SHORT_SHA#g" k8s/deployment.yaml
        kubectl apply -k k8s
        kubectl rollout status deployment/$_SERVICE_NAME -n "$_GKE_NAMESPACE" --timeout=1200s
